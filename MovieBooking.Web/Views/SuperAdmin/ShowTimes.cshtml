@* @model List<MovieBooking.Web.ApiContracts.ShowTimes.ShowTimeResponse>
<h2 class="mt-4">⏰ ShowTimes</h2>

<a asp-action="AddShowTime" class="btn btn-primary mb-3">➕ Add ShowTime</a>

<table class="table table-bordered shadow-sm">
    <thead class="table-dark">
        <tr>
            <th>Movie</th>
            <th>Theatre</th>
            <th>Screen</th>
            <th>Language</th>
            <th>Time</th>
            <th>Price</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var st in Model)
        {
            <tr>
                <td>@st.MovieTitle</td>
                <td>@st.TheatreName</td>
                <td>@st.ScreenName</td>
                <td>@st.LanguageName</td>
                <td>@st.StartTime.ToString("dd-MM-yyyy HH:mm")</td>
                <td>₹@st.BasePrice</td>
            </tr>
        }

    </tbody>
</table> *@
@model List<MovieBooking.Web.ApiContracts.ShowTimes.ShowTimeResponse>

@{
    ViewData["Title"] = "ShowTimes";
}

<style>
    .showtime-card {
        border-radius: 18px;
        transition: transform 0.25s ease, box-shadow 0.25s ease;
        border: none;
    }

        .showtime-card:hover {
            transform: translateY(-6px);
            box-shadow: 0 20px 35px rgba(0,0,0,0.12);
        }

    .showtime-title {
        font-weight: 600;
        font-size: 1.05rem;
    }

    .showtime-meta {
        font-size: 0.9rem;
        color: #6c757d;
    }

    .price-badge {
        font-size: 0.85rem;
        padding: 6px 12px;
        border-radius: 12px;
    }

    .empty-state {
        border: 2px dashed #dee2e6;
        border-radius: 18px;
        padding: 40px;
        text-align: center;
        background-color: #fafafa;
    }
</style>

<div class="container mt-4">

    <!-- HEADER -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="fw-bold mb-0">⏰ ShowTimes</h2>
            <small class="text-muted">Manage movie showtimes</small>
        </div>

        <a asp-action="AddShowTime" class="btn btn-primary fw-semibold">
            <i class="bi bi-plus-lg"></i> Add ShowTime
        </a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show">
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (!Model.Any())
    {
        <!-- EMPTY STATE -->
        <div class="empty-state shadow-sm">
            <h5 class="fw-bold">No ShowTimes Available</h5>
            <p class="text-muted mb-3">
                Start by adding showtimes for movies.
            </p>
            <a asp-action="AddShowTime" class="btn btn-outline-primary">
                ➕ Add ShowTime
            </a>
        </div>
    }
    else
    {
        <!-- SHOWTIME CARDS -->
        <div class="row g-4">
            @foreach (var st in Model)
            {
                <div class="col-xl-3 col-lg-4 col-md-6">
                    <div class="card showtime-card shadow-sm h-100">
                        <div class="card-body d-flex flex-column">

                            <!-- MOVIE TITLE -->
                            <div class="showtime-title mb-2">
                                <i class="bi bi-film text-muted me-1"></i>
                                @st.MovieTitle
                            </div>

                            <!-- META -->
                            <div class="showtime-meta mb-3">
                                <div>
                                    <i class="bi bi-building"></i>
                                    @st.TheatreName
                                </div>
                                <div>
                                    <i class="bi bi-tv"></i>
                                    @st.ScreenName
                                </div>
                                <div>
                                    <i class="bi bi-translate"></i>
                                    @st.LanguageName
                                </div>
                                <div>
                                    <i class="bi bi-clock"></i>
                                    @st.StartTime.ToString("dd MMM yyyy, hh:mm tt")
                                </div>
                            </div>

                            <!-- PRICE -->
                            <div class="mb-3">
                                <span class="badge bg-success price-badge">
                                    ₹ @st.BasePrice
                                </span>
                            </div>

                            <!-- ACTIONS -->
                            <div class="mt-auto d-flex gap-2">
                                <a asp-action="EditShowTime"
                                   asp-route-id="@st.ShowTimeId"
                                   class="btn btn-sm btn-outline-primary flex-fill">
                                    <i class="bi bi-pencil"></i> Edit
                                </a>

                                <button type="button"
                                        class="btn btn-sm btn-outline-danger flex-fill"
                                        onclick="confirmDelete('@st.ShowTimeId', '@st.MovieTitle')">
                                    <i class="bi bi-trash"></i> Delete
                                </button>
                            </div>

                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>

<!-- DELETE CONFIRMATION MODAL -->
<div class="modal fade" id="deleteModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Confirm Delete</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                Are you sure you want to delete showtime for
                <strong id="movieName"></strong>?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    Cancel
                </button>
                <form id="deleteForm" method="post">
                    <button type="submit" class="btn btn-danger">
                        Delete
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function confirmDelete(showTimeId, movieTitle) {
            document.getElementById('movieName').textContent = movieTitle;
            document.getElementById('deleteForm').action =
                '/SuperAdmin/DeleteShowTime/' + showTimeId;

            var modal = new bootstrap.Modal(
                document.getElementById('deleteModal')
            );
            modal.show();
        }
    </script>
}
